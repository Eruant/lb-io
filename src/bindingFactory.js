export default (state) => {
  return {
    addBinding ({name, action, device} = {}) {
      // name = 'run'
      // action = 'keydown && keyCode = 13'
      // device = 'keyboard'
    },
    removeBinding () {
    },
    reassignBinding () {
    }
  }
}
